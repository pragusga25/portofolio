import Container from '@components/Container';
import { FC } from 'react';
import tw from 'twin.macro';

export const About: FC = () => (
  <Container>
    <div tw="flex justify-center items-center text-4xl">ABOUT</div>
  </Container>
);
